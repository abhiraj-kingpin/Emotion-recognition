fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
numpy==2.4.6
librosa==0.11.0
soundfile==0.14.0
joblib==1.5.3
scikit-learn==1.9.0
# ai-edge-litert (not tensorflow-cpu): a standalone ~47MB TFLite interpreter
# instead of the ~1.5GB full TensorFlow package. Serving from
# ml/models/cnn_model.tflite this way is what keeps the backend inside
# free-tier RAM limits (Render/Railway 512MB) -- see ml/predict.py's
# _load_cnn() for the fallback chain if you ever need full TF here instead.
ai-edge-litert==2.1.6
pydantic==2.13.4
